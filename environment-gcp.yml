# run: conda env create --file environment.yml

name: lemmatizer-env
channels:
  - conda-forge
dependencies:
  - gensim
  - keras
  - tensorflow-gpu=1.14
  - python-Levenshtein
  - scikit-learn
  - smart_open
  - pytest
  - pydot
  - graphviz
  - tqdm
  - pip
  - pip:
    - py-dom-xpath-six
